let num: number;
num = 20;
let cad: string;
cad = "hola";
let bool: boolean;
let arr: number[];
let obj: {
    edad: number,
    nombre: string,
    otra: boolean
};

let persona: {
    edad: number;
    nombre: string;
    pasatiempos: {
        extremos: string[];
        comunes: {
            enPareja: string[];
            solo: string[]
        }
    };
    esCasado: boolean
};

console.log(num);
console.log(cad);